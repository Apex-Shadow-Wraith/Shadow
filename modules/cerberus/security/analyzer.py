"""Cerberus security analyzer — placeholder.

Filled in commit 2 with the verbatim port of
modules/sentinel/security_analyzer.py (firewall configuration
analysis, evaluation, comparison, rule explanation, generation,
plus the security-knowledge Grimoire writer). Logger renames
shadow.sentinel.analyzer -> shadow.cerberus.security.analyzer
and source_module="sentinel" -> "cerberus.security" land in
that commit.
"""
